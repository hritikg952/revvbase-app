---
phase: 03-seller-listing-crud
plan: 01
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - src/app/sell/page.tsx
  - src/app/my-listings/page.tsx
  - src/app/listings/[id]/edit/page.tsx
  - src/components/listing-form.tsx
  - src/components/my-listing-card.tsx
  - src/lib/listings.ts
autonomous: true
requirements: [LIST-01, LIST-02, LIST-03, LIST-04, LIST-05]
must_haves:
  truths:
    - "Authenticated users can publish all required listing fields."
    - "Created rows use the current Supabase user ID as seller_id."
    - "Owners can view, edit, and soft-delete their own listings."
    - "Cross-owner mutation remains impossible through RLS."
    - "Missing listing images use the shared stock placeholder."
---

<objective>
Implement the smallest usable seller workflow on top of Phase 1 RLS and Phase 2 Auth.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build validated listing create/edit form and Data API functions</name>
  <action>Implement typed mapping, browser validation, authenticated insert/update, route protection UX, and clear mutation feedback for every required field.</action>
  <verify>Unit tests cover validation and payload mapping; hosted authenticated creation returns a row owned by the current user.</verify>
</task>
<task type="auto">
  <name>Task 2: Build minimal owner management and soft deletion</name>
  <action>Query owner-visible rows, render status-aware controls, load owned rows into edit, and set status to deleted after confirmation. Handle loading, empty, error, and no-session states.</action>
  <verify>Hosted user can edit and soft-delete own row; second user mutation is rejected/affects zero rows.</verify>
</task>
</tasks>

<success_criteria>
LIST-01 through LIST-05 have direct automated and hosted runtime evidence.
</success_criteria>

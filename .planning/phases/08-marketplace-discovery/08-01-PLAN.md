---
phase: 08-marketplace-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/listing-discovery.ts
  - src/lib/listing-discovery.test.ts
  - src/components/listing-filters.tsx
  - src/components/listings-feed.tsx
  - src/app/globals.css
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All discovery controls produce one normalized query model rather than issuing individual Supabase predicates."
    - "Applied filters are represented in a stable URL and can later accept a text search value."
    - "Desktop and mobile filter controls remain usable while their filter body scrolls."
    - "Supabase receives normalized range, multi-select, search, sort, and pagination values."
  artifacts:
    - path: src/lib/listing-discovery.ts
      provides: Query type, URL codec, popular-pill configuration, and public Supabase discovery fetch
    - path: src/components/listing-filters.tsx
      provides: Sticky popular row, desktop sidebar, and mobile bottom-sheet controls
    - path: src/lib/listing-discovery.test.ts
      provides: Pure query normalization and URL round-trip coverage
  key_links:
    - from: src/components/listing-filters.tsx
      to: src/lib/listing-discovery.ts
      via: Query draft and canonical URL serialization
    - from: src/components/listings-feed.tsx
      to: src/lib/listing-discovery.ts
      via: One paginated public-listing fetch function
---

<objective>
Implement Phase 8 Marketplace Discovery as one reusable query architecture with responsive filtering.

Output: pure query/URL utilities, Supabase-backed paginated listing reads, sticky desktop/mobile filter UI, and regression tests.
</objective>

<execution_context>
@/Users/peakysmokin14/.codex/gsd-core/workflows/execute-plan.md
@/Users/peakysmokin14/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-marketplace-discovery/08-CONTEXT.md
@.planning/phases/08-marketplace-discovery/08-RESEARCH.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Build the shared discovery query and URL contract</name>
  <files>src/lib/listing-discovery.ts, src/lib/listing-discovery.test.ts</files>
  <behavior>
    - Query values normalize, deduplicate, and sort deterministically.
    - URL parsing and serialization round-trip structured filters, sort, pagination, and future text search.
    - Range values reject invalid numbers and preserve valid bounds.
    - Popular pills map to the same canonical vehicle/fuel query fields.
    - Public Supabase reads apply AND across categories, OR within multi-value categories, sorting, and pagination.
  </behavior>
  <action>Implement ListingDiscoveryQuery, pure normalization and URL codec helpers, configured popular filters, safe text predicate construction, and fetchPublicListings using the browser Supabase client. Keep database-specific predicate construction inside this module and select the existing listing fields required by cards.</action>
  <verify><automated>npm test -- src/lib/listing-discovery.test.ts && npm run typecheck</automated></verify>
  <done>The future search bar can set query.text without changing filter components or the Supabase call site.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the feed fetch with the discovery contract</name>
  <files>src/components/listings-feed.tsx</files>
  <behavior>
    - Initial query comes from the current URL.
    - Applying detailed filters updates the URL and reloads the first page.
    - Loading more requests the next page through the same query function.
    - Existing loading, error, empty, card, and image-placeholder behavior remains available.
  </behavior>
  <action>Refactor ListingsFeed to load through fetchPublicListings, react to URL changes, preserve image projection, and add a lightweight Load more control for paginated results.</action>
  <verify><automated>npm test && npm run typecheck</automated></verify>
  <done>The feed never constructs filter predicates directly and remains compatible with a future search input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add responsive filter controls and sticky layout</name>
  <files>src/components/listing-filters.tsx, src/components/listings-feed.tsx, src/app/globals.css</files>
  <behavior>
    - Popular pills apply immediately and use horizontal scrolling while the master filter button remains visible.
    - Desktop filter sidebar stays below the sticky header with a scrolling body and permanently visible Clear/Apply footer.
    - Mobile opens a bottom sheet with a scrolling body and permanently visible Apply footer.
    - Multi-select controls support vehicle type, fuel, make, model, city, ranges, and previous owners.
    - Clear resets draft/applied state and removes URL filters.
  </behavior>
  <action>Build accessible filter controls from the shared query model, use the existing catalog for makes/models, derive city options from loaded listings, synchronize applied state to the canonical URL, and add responsive styles that preserve sticky controls without introducing a second filter implementation.</action>
  <verify><automated>npm test && npm run typecheck && npm run build</automated></verify>
  <done>Visitors can filter listings on desktop and mobile with pinned actions and a future-compatible query state.</done>
</task>
</tasks>

<verification>Run focused query tests, the full Vitest suite, typecheck, and production build. Manually verify desktop and mobile sticky behavior with URL refresh/back navigation.</verification>
<success_criteria>Marketplace discovery is query-driven, URL-backed, paginated, responsive, and ready for a future text search input.</success_criteria>
<output>Create `.planning/phases/08-marketplace-discovery/08-01-SUMMARY.md` when done</output>

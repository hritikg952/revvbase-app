---
phase: 01-supabase-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/migrations/20260730170000_create_mvp_schema.sql
  - supabase/migrations/20260730173000_harden_function_and_read_policies.sql
  - supabase/seed.sql
  - supabase/tests/rls.sql
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "The hosted project schema is reproducible from committed Supabase migrations."
    - "Anonymous users can read active listings and catalog rows but cannot write."
    - "Authenticated owners can insert, read, update, and soft-delete only their own listings."
    - "No service-role credential is committed or exposed to browser configuration."
  artifacts:
    - path: "supabase/migrations/20260730170000_create_mvp_schema.sql"
      provides: "MVP tables, constraints, indexes, triggers, grants, and RLS policies"
    - path: "supabase/migrations/20260730173000_harden_function_and_read_policies.sql"
      provides: "Security-definer function privilege hardening and non-overlapping read policies"
    - path: "supabase/seed.sql"
      provides: "Deterministic vehicle catalog seed data"
    - path: "supabase/tests/rls.sql"
      provides: "Hosted SQL assertions for anonymous and cross-owner access"
  key_links:
    - from: "public.listings.seller_id"
      to: "auth.users.id"
      via: "foreign key and auth.uid()-based policies"
    - from: "public active listing reads"
      to: "listings.status"
      via: "RLS predicate status = 'active'"
---

<objective>
Create and deploy the reproducible Supabase schema, reference seed, and security policies required by the Revvbase MVP.

Purpose: establish the database and authorization boundary that every later web phase relies on.
Output: committed Supabase artifacts applied and verified against project `qokumaemcqwkqhrxyolc`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE-MVP.md
@.planning/phases/01-supabase-foundation/01-CONTEXT.md
@.planning/phases/01-supabase-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the Supabase project and MVP schema migration</name>
  <files>supabase/config.toml, supabase/migrations/20260730170000_create_mvp_schema.sql, supabase/migrations/20260730173000_harden_function_and_read_policies.sql</files>
  <action>Create profiles, vehicle_catalog, and listings with reviewed constraints, ownership, timestamps, indexes, grants, triggers, and RLS. Public reads must expose only active listings; owner policies must constrain both existing and resulting rows. Hard delete remains unavailable so deletion is represented by status.</action>
  <verify>Inspect SQL for enabled RLS on every public table, auth.uid() ownership predicates, and no privileged credentials.</verify>
  <done>The full database contract is represented by a forward migration.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic catalog seed and hosted RLS assertions</name>
  <files>supabase/seed.sql, supabase/tests/rls.sql</files>
  <action>Seed representative Indian two-wheeler makes/models with deterministic IDs. Add transactional SQL assertions for anonymous read/write behavior, public deleted-row filtering, owner CRUD, and cross-owner denial.</action>
  <verify>Run the RLS assertion script against the linked hosted database after applying migration and seed.</verify>
  <done>Reference data and policy behavior are reproducibly testable.</done>
</task>

<task type="auto">
  <name>Task 3: Link, push, and verify the hosted project</name>
  <files>supabase/.temp/project-ref</files>
  <action>Link the CLI to `qokumaemcqwkqhrxyolc`, push all migrations with the seed, then execute the policy assertions. Retrieve credentials only from the authenticated CLI/macOS Keychain at runtime; never write them to tracked files.</action>
  <verify>`supabase migration list` shows the migration remotely and the hosted RLS assertion transaction completes without exception.</verify>
  <done>The hosted project matches the committed schema and passes security checks.</done>
</task>

</tasks>

<verification>
- Supabase migration history matches the repository.
- Seed rows are present in `vehicle_catalog`.
- Anonymous writes fail.
- Owner insert/update/soft-delete succeeds.
- Cross-owner update and delete attempts affect zero rows.
- Repository scans find no service-role key.
</verification>

<success_criteria>
All Phase 1 roadmap success criteria and SEC-01 through SEC-03 have direct hosted evidence.
</success_criteria>

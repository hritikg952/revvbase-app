---
phase: 05-mvp-validation-and-deployment
plan: 01
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - README.md
  - supabase/config.toml
  - .planning/phases/05-mvp-validation-and-deployment/05-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The local runtime serves the responsive Revvbase marketplace against hosted Supabase."
    - "Browser configuration contains only Supabase publishable values."
    - "A public user can sign up, publish, sign out, and see the active listing."
    - "All local, hosted security, and browser smoke checks pass."
---

<objective>
Independently verify the complete Revvbase MVP locally against its hosted Supabase backend, without deploying the web application.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Run release gates and document operation</name>
  <action>Run typecheck, unit tests, production build, full npm audit, remote migration/advisor checks, and hosted Auth/CRUD/RLS acceptance. Document local setup, safe environment variables, database deployment, and verification commands.</action>
  <verify>Every command exits successfully and no privileged values appear in tracked files.</verify>
</task>
<task type="auto">
  <name>Task 2: Verify the hosted backend boundary</name>
  <action>Confirm migration parity, run RLS SQL checks and Supabase advisors, execute the hosted Auth/CRUD/RLS acceptance test, confirm representative synthetic demo inventory, and document any non-blocking platform hardening recommendations.</action>
  <verify>Hosted database, Auth, owner isolation, and public-read filtering pass without leaving temporary users or data; advisor output is reviewed and classified.</verify>
</task>
<task type="auto">
  <name>Task 3: Complete local browser smoke testing</name>
  <action>Run the application locally against hosted Supabase and inspect the signed-out feed, authentication/session flow, seller create/edit/delete controls, and representative mobile layout. Record final requirement evidence.</action>
  <verify>The complete seller/public loop works in the local browser with no horizontal overflow or runtime crash.</verify>
</task>
</tasks>

<success_criteria>
Every Phase 5 roadmap criterion is proven locally against the live hosted Supabase backend. Web hosting remains explicitly deferred.
</success_criteria>

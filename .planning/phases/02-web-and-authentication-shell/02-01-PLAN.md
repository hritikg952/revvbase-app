---
phase: 02-web-and-authentication-shell
plan: 01
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - package.json
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/auth/page.tsx
  - src/components/site-header.tsx
  - src/components/auth-form.tsx
  - src/components/auth-provider.tsx
  - src/lib/supabase.ts
  - src/lib/database.types.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
must_haves:
  truths:
    - "The responsive public site shell renders without authentication."
    - "A user can create an account and sign in with email/password."
    - "The authenticated session survives reload and the user can sign out."
    - "No buyer/seller role selection exists and browser configuration contains no privileged key."
---

<objective>
Build the production-shaped Next.js shell and email/password authentication foundation on the hosted Supabase project.
</objective>

<context>
@.planning/phases/02-web-and-authentication-shell/02-CONTEXT.md
@.planning/phases/02-web-and-authentication-shell/02-RESEARCH.md
@.planning/phases/02-web-and-authentication-shell/02-UI-SPEC.md
@.planning/phases/01-supabase-foundation/01-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Scaffold the Next.js application and shared visual shell</name>
  <action>Create a current App Router TypeScript application, environment validation, metadata, responsive header, shared CSS tokens, accessible controls, and a public landing route.</action>
  <verify>Install dependencies, run lint/type checking, and build with publishable Supabase environment variables.</verify>
</task>
<task type="auto">
  <name>Task 2: Implement persistent Supabase email/password authentication</name>
  <action>Create the singleton browser client, Auth provider, sign-in/sign-up modes, loading/error/success feedback, authenticated header state, and sign-out. Do not implement roles or use service credentials.</action>
  <verify>Automated tests exercise auth form state and provider-driven header states; a hosted test account can sign up, persist across reload, and sign out.</verify>
</task>
</tasks>

<verification>
- Production build passes.
- Browser bundle environment names are limited to the public URL and publishable key.
- Public shell is reachable when signed out.
- Hosted sign-up/sign-in/session/sign-out flow works.
</verification>

<success_criteria>
AUTH-01 through AUTH-04 have direct automated or hosted runtime evidence.
</success_criteria>

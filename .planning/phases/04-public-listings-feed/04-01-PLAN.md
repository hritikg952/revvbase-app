---
phase: 04-public-listings-feed
plan: 01
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - src/app/page.tsx
  - src/components/listings-feed.tsx
  - src/components/listing-card.tsx
  - public/vehicle-placeholder.svg
autonomous: true
requirements: [BROW-01, BROW-02, BROW-03]
must_haves:
  truths:
    - "A signed-out visitor can load active listings directly from Supabase."
    - "Cards identify the vehicle, price, city, and image or local placeholder."
    - "Loading, empty, and error states are visible and actionable."
    - "Soft-deleted rows never render in the public feed."
---

<objective>
Deliver the signed-out marketplace browsing experience and complete the MVP discovery side of the loop.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build the public active-listings query and stateful feed</name>
  <action>Fetch newest active listings through the publishable Supabase client and render explicit loading, empty, error/retry, and populated states without requiring a session.</action>
  <verify>Component tests cover every state and assert the query filters active rows.</verify>
</task>
<task type="auto">
  <name>Task 2: Build responsive listing cards and local placeholder</name>
  <action>Render semantic cards with vehicle identity, year, kilometres, city, formatted INR price, and robust local placeholder behavior.</action>
  <verify>Production build succeeds and runtime inspection confirms responsive feed behavior with signed-out seeded data.</verify>
</task>
</tasks>

<success_criteria>
BROW-01 through BROW-03 have automated and hosted runtime evidence, and deleted rows remain absent.
</success_criteria>

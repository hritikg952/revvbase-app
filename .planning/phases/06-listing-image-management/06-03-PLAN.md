---
phase: 06-listing-image-management
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - supabase/migrations/20260801010000_cut_over_listing_image_contract.sql
  - supabase/tests/listing-images-rls.sql
  - src/lib/database.types.ts
  - src/lib/listings.ts
autonomous: false
requirements: [IMG-03, IMG-04]
must_haves:
  truths:
    - "The old single external image URL is no longer a competing listing-media source."
    - "The linked Supabase project has the current image schema and strict bucket/database policy mirrors before hosted validation proceeds."
  artifacts:
    - path: supabase/migrations/20260801010000_cut_over_listing_image_contract.sql
      provides: Legacy contract removal and permanent listing delete authorization
    - path: supabase/tests/listing-images-rls.sql
      provides: Hosted policy/cap/cleanup assertions
  key_links:
    - from: permanent listing cleanup service
      to: authorized listing DELETE policy
      via: object cleanup succeeds before metadata cascade and hard row delete
---

<objective>
Complete the irreversible legacy media cutover and apply the schema to the hosted Supabase project before final acceptance.

Purpose: Remove the arbitrary single-URL contract only after all app consumers use ordered metadata, then make the hosted schema the source of truth.
Output: Forward cutover migration, hard-delete authorization, applied hosted migration, and RLS evidence.

## Artifacts this phase produces

- A deliberate forward migration that retires the old `listings.image_url` data contract.
- Hosted Supabase migration parity and executable image RLS/cap regression evidence.
</objective>

<execution_context>
@/Users/peakysmokin14/.codex/gsd-core/workflows/execute-plan.md
@/Users/peakysmokin14/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-listing-image-management/06-CONTEXT.md
@.planning/phases/06-listing-image-management/06-RESEARCH.md
@.planning/phases/06-listing-image-management/06-VALIDATION.md
@.planning/phases/06-listing-image-management/06-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Approve removal of the legacy `listings.image_url` contract and replacement of owner soft deletion with the planned permanent image/listing cleanup flow.</decision>
  <context>This is a one-way data-contract change: reverting after release needs another migration and cannot recover already-deleted storage objects. Phase 6 UI consumers must already use `listing_images`; inspect existing hosted values and confirm they are intentionally discarded or have an approved data migration path.</context>
  <options>
    <option id="approve-cutover">
      <name>Approve cutover</name>
      <pros>Eliminates arbitrary external URLs and one competing image source.</pros>
      <cons>Legacy values and successful deleted images cannot be recovered through the application.</cons>
    </option>
    <option id="migrate-legacy-values">
      <name>Migrate legacy data first</name>
      <pros>Preserves existing images where source ownership and licensing permit it.</pros>
      <cons>Requires a separately approved import/lifecycle design before destructive migration.</cons>
    </option>
  </options>
  <resume-signal>Select `approve-cutover` or `migrate-legacy-values`.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: [BLOCKING] apply and prove the Supabase image-contract migration</name>
  <precondition>The preceding decision approved the exact legacy-data disposition, and the Supabase CLI is authenticated and linked to project `qokumaemcqwkqhrxyolc`.</precondition>
  <reversibility rating="one-way">Dropping the legacy column and authorizing permanent deletion changes persistent data and requires a forward migration to undo, per D-03 and D-08.</reversibility>
  <files>supabase/migrations/20260801010000_cut_over_listing_image_contract.sql, supabase/tests/listing-images-rls.sql, src/lib/database.types.ts, src/lib/listings.ts</files>
  <action>After the decision, write a new forward-only cutover migration that removes `listings.image_url` and its constraints only after checking the approved legacy-data disposition; grants owner-scoped listing DELETE only as needed for the already-implemented cleanup order; and preserves the additive `listing_images` relation, public read, owner storage policy, durable cap, and no-hardcoded-config deployment mirror. Update residual app types/helpers so no `image_url` field remains. Then run the blocking hosted operation: `supabase db push`, `supabase migration list`, and `supabase test db --linked` (or the repository’s equivalent linked SQL runner) with `supabase/tests/listing-images-rls.sql`. Extend assertions for anonymous public object access, owner upload/delete, different-user denial, sixth-image denial, and cleanup sequencing. Halt final verification on any push/parity/RLS failure; never use a service-role key in browser code or tracked files.</action>
  <verify><automated>supabase db push && supabase migration list && supabase test db --linked && npm test -- src/lib/listing-images.test.ts src/lib/listing-image-storage.test.ts && npm run typecheck</automated></verify>
  <done>The linked project has the forward migration applied, no application or schema contract retains `image_url`, and hosted policy tests prove public read plus strict owner mutation/cap enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Migration author → hosted database | A persistent schema/data decision changes the production-like project. |
| Object service → database delete | Storage bytes and relational rows cannot share one transaction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-10 | Tampering | migration/cutover | high | mitigate | Blocking human decision, forward-only migration, explicit legacy-data inspection, migration parity, and RLS regression. |
| T-06-11 | Elevation | listing DELETE policy | high | mitigate | Authenticated owner `auth.uid() = seller_id` policy and cross-user hosted denial test. |
| T-06-12 | Denial of service | orphan object cleanup | high | mitigate | Application cleanup runs object removal before authorized DB deletion; SQL only cascades metadata after byte cleanup. |
| T-06-13 | Repudiation | hosted schema apply | medium | mitigate | Committed migration, linked migration list, and repeatable test output establish audit trail. |
</threat_model>

<verification>
This plan is the blocking schema-push gate. Do not begin final browser/hosted acceptance until `supabase db push`, migration parity, linked SQL checks, focused tests, and typecheck all pass.
</verification>

<success_criteria>
- The legacy single URL is removed only after explicit approval and no listing consumer depends on it.
- The linked hosted project, not just repository SQL, enforces public reads and owner-only upload/delete/count behavior.
- Permanent listing deletion follows storage cleanup, then metadata/listing deletion.
</success_criteria>

<output>Create `.planning/phases/06-listing-image-management/06-03-SUMMARY.md` when done</output>

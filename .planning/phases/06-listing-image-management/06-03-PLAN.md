---
phase: 06-listing-image-management
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - src/lib/listing-image-lifecycle-client.ts
  - supabase/functions/listing-image-cleanup/index.ts
  - supabase/functions/listing-image-cleanup/lifecycle.ts
  - supabase/functions/listing-image-cleanup/lifecycle.test.ts
  - supabase/tests/listing-images-rls.sql
  - supabase/tests/hosted-listing-image-cleanup.ts
autonomous: false
requirements: [IMG-03, IMG-04]
user_setup:
  - service: Supabase
    why: "The lifecycle Edge Function needs a server-only service-role secret and deployed migration/policy mirror."
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API; configure only as an Edge Function secret."
must_haves:
  truths:
    - "A draft publishes with zero photos only while the deployed required-image policy is false; when true, it remains draft until it has a persisted image."
    - "Deleting the final photo from an active required-image listing immediately leaves the listing safely draft, never active with zero photos."
    - "Listing-wide cleanup permanently removes objects and metadata before setting the retained listing row to deleted."
  artifacts:
    - path: supabase/functions/listing-image-cleanup/index.ts
      provides: JWT-validated publish and cleanup endpoint
    - path: supabase/tests/hosted-listing-image-cleanup.ts
      provides: Two-policy-mode hosted lifecycle harness
  key_links:
    - from: src/lib/listing-image-lifecycle-client.ts
      to: supabase/functions/listing-image-cleanup/index.ts
      via: Authenticated Edge Function invocation
---

<objective>
Implement the protected, configuration-driven listing publication and permanent-cleanup lifecycle.

Purpose: Make D-08 and D-11 correct under real object-store failure modes without client status authority.
Output: Lifecycle client/function, injected-order unit tests, and hosted two-mode security harness.
</objective>

<execution_context>
@/Users/peakysmokin14/.codex/gsd-core/workflows/execute-plan.md
@/Users/peakysmokin14/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-listing-image-management/06-CONTEXT.md
@.planning/phases/06-listing-image-management/06-RESEARCH.md
@.planning/phases/06-listing-image-management/06-VALIDATION.md
@.planning/phases/06-listing-image-management/06-02-SUMMARY.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Publish one owner draft through the required-image policy</name>
  <files>src/lib/listing-image-lifecycle-client.ts, supabase/functions/listing-image-cleanup/index.ts, supabase/functions/listing-image-cleanup/lifecycle.ts, supabase/functions/listing-image-cleanup/lifecycle.test.ts</files>
  <behavior>
    - With policy false, an owned draft with zero images returns active; with policy true it remains draft with a clear precondition error until one metadata row exists, then returns active.
    - Missing/invalid JWTs and non-owner listing IDs are rejected; clients cannot supply or override the policy value.
    - The browser helper forwards only typed lifecycle actions and has no service-role credential.
  </behavior>
  <action>Implement D-10 and D-11 in a JWT-validated Supabase Edge Function named `listing-image-cleanup`, retaining cleanup authority while adding a narrowly typed `publish` action. Authenticate the bearer session, verify owner before elevated work, read `images.required` from the migration-owned database policy mirror rather than trusting browser input, count persisted `listing_images`, and transition only the caller's draft to active when its rule is satisfied. Factor logic behind injected database/Storage interfaces for Deno tests. Keep the browser helper limited to authenticated invocation and returned domain status; never expose a service-role client, raw bucket operation, or policy override.</action>
  <verify><automated>deno test supabase/functions/listing-image-cleanup/lifecycle.test.ts</automated></verify>
  <done>Protected activation enforces both configuration modes and reports a typed draft-preserving failure when required images are missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protect final-photo removal and listing-wide cleanup sequencing</name>
  <files>supabase/functions/listing-image-cleanup/lifecycle.ts, supabase/functions/listing-image-cleanup/lifecycle.test.ts, supabase/tests/listing-images-rls.sql, supabase/tests/hosted-listing-image-cleanup.ts</files>
  <behavior>
    - Removing a non-final photo removes its object then metadata.
    - With required policy true, removing an active listing's final photo first changes it to draft, then removes object then metadata; any later removal failure leaves it draft with intact retryable metadata/object where applicable.
    - Listing-wide cleanup enumerates authoritative keys, removes all objects, deletes metadata, then changes the row to deleted; an object failure leaves listing status/metadata unchanged for that workflow.
    - Hosted tests prove owner draft visibility, no anonymous/non-owner public draft record or metadata visibility, both publication modes, forged-key/direct mutation denial, and each sequence; they do not treat public draft object URLs as private.
  </behavior>
  <action>Implement D-07, D-08, and D-11 with explicit actions for individual-image deletion, listing deletion, and failed-registration compensation. For the final image of an active required listing, first perform the protected active-to-draft transition; then remove the object through Storage and delete its metadata. If object/metadata removal subsequently fails, return a retryable result that truthfully leaves the record draft rather than attempting to reactivate it. For listing deletion retain the mandated object → metadata → `deleted` order and leave status/rows intact if object deletion fails. Validate owner/listing/key binding for every action. Extend SQL and hosted Deno harnesses to run the policy mirror in both false and true fixtures and assert no public draft listing/metadata response, direct browser status/delete denial, and post-cleanup CDN behavior after cache propagation. Do not implement private draft staging or promotion; draft object bytes remain public by this MVP decision.</action>
  <verify><automated>deno test supabase/functions/listing-image-cleanup/lifecycle.test.ts</automated></verify>
  <done>Final-photo removal has no active-empty state, listing-wide cleanup keeps required permanent ordering, and automated/hosted harnesses cover both configuration modes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: deploy and run the protected lifecycle harness</name>
  <action>Deploy the migration, policy mirror, Edge Function, and two-mode hosted harness to the linked Supabase project.</action>
  <instructions>Set `SUPABASE_SERVICE_ROLE_KEY` only as an Edge Function secret, run `supabase db push`, `supabase secrets set SUPABASE_SERVICE_ROLE_KEY`, and `supabase functions deploy listing-image-cleanup`; configure non-privileged owner/non-owner test identities, then run `deno run --allow-env --allow-net supabase/tests/hosted-listing-image-cleanup.ts`.</instructions>
  <verification>Confirm output separately proves required=false zero-image activation, required=true draft rejection then activation after image persistence, draft record/metadata absence from public reads, final-photo draft reversion, direct mutation denial, forged-key rejection, public draft object delivery as accepted MVP behavior, and object/metadata/deleted listing cleanup ordering.</verification>
  <resume-signal>Type `deployed and passed` or provide the deployment/harness failure output.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser → lifecycle function | Caller supplies action and resource IDs but not policy authority. |
| Edge Function → database/Storage | Elevated authority spans transactional status rows and non-transactional objects. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-07 | Elevation | publish/delete actions | high | mitigate | JWT verification, owner lookup, fixed action schema, and server-side policy lookup. |
| T-06-08 | Tampering | final-photo state | high | mitigate | Pre-delete protected transition to draft when required and final image. |
| T-06-09 | Tampering | listing cleanup | high | mitigate | Enumerate authoritative metadata, object-first cleanup, and only then deleted status. |
| T-06-10 | Information disclosure | draft records and metadata | high | mitigate | Hosted no-public-draft-record/metadata assertions for anon and another user. Public draft object delivery is accepted MVP scope; private staging/promotion is deferred. |
</threat_model>
<verification>Run Deno unit tests per task and the blocking hosted harness before UI wiring; Plan 06 repeats it in final release validation.</verification>
<success_criteria>One protected endpoint owns publish and permanent cleanup, honors both settings modes, and cannot leave an active required listing without images.</success_criteria>
<output>Create `.planning/phases/06-listing-image-management/06-03-SUMMARY.md` when done</output>

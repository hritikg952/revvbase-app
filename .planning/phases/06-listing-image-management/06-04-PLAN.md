---
phase: 06-listing-image-management
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - src/components/listing-image-manager.tsx
  - src/components/listing-form.tsx
  - src/app/listings/[id]/edit/page.tsx
  - src/lib/listing-images.test.ts
  - src/app/globals.css
autonomous: true
requirements: [IMG-01, IMG-02, IMG-05]
must_haves:
  truths:
    - "A seller creates a draft, then receives a config-driven publication outcome: immediate active without photos only when the setting is false, otherwise clear draft guidance until a photo persists."
    - "An owner can add/delete persisted photos independently of unsaved text and sees an accurate draft result after final required-photo removal."
    - "Seller-only draft management never makes draft data a public-card concern."
  artifacts:
    - path: src/components/listing-image-manager.tsx
      provides: Accessible immediate photo manager with lifecycle status feedback
    - path: src/components/listing-form.tsx
      provides: Draft-create and protected-publish flow
  key_links:
    - from: src/components/listing-form.tsx
      to: src/lib/listing-image-lifecycle-client.ts
      via: Protected publish action after draft persistence
---

<objective>
Deliver the immediate, accessible seller photo manager and config-driven draft publication UX.

Purpose: Make D-07, D-09, D-10, and D-11 understandable without coupling forms to Supabase storage details.
Output: Draft-aware create/edit form, photo manager, state regressions, and responsive styles.
</objective>

<execution_context>
@/Users/peakysmokin14/.codex/gsd-core/workflows/execute-plan.md
@/Users/peakysmokin14/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-listing-image-management/06-CONTEXT.md
@.planning/phases/06-listing-image-management/06-UI-SPEC.md
@.planning/phases/06-listing-image-management/06-03-SUMMARY.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Create a listing through draft then protected publication</name>
  <files>src/components/listing-form.tsx, src/app/listings/[id]/edit/page.tsx, src/lib/listing-images.test.ts</files>
  <behavior>
    - Form creation persists draft first and invokes protected publication; with `images.required=false` it reaches active with zero photos, while true routes to the persisted draft/edit page with a clear add-photo requirement.
    - A draft edit page is owner reachable; it exposes no public-card link or implication that it is publicly visible.
    - Existing active listing field edits retain status and do not bypass protected lifecycle actions.
  </behavior>
  <action>Implement D-09, D-10, and D-11. Remove legacy editable `image_url`; create through the draft-only schema path, then call the lifecycle client rather than writing `status` from the form. Consume the typed JSON setting solely for explanatory copy and correct route flow, but render the server result as authoritative. For required=true, route the owner to the persisted edit page and present its draft/add-photo state; after an image succeeds, offer/perform the protected publish action. Query edit data through owner access so drafts render there, without adding a public route/query for drafts.</action>
  <verify><automated>npm test -- src/lib/listing-images.test.ts && npm run typecheck</automated></verify>
  <done>Create flow is draft-first, config-aware, server-authoritative, and makes zero-image publication available only under the false mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add independent photo controls and final-photo draft feedback</name>
  <files>src/components/listing-image-manager.tsx, src/components/listing-form.tsx, src/lib/listing-images.test.ts, src/app/globals.css</files>
  <behavior>
    - A persisted listing accepts a prepared photo while modified text remains unsaved; first ordered image is Cover and per-file failures leave ready tiles unchanged.
    - At capacity, invalid file, cleanup failure, and final required-photo deletion, the manager shows accurate per-file/status feedback and preserves retryable state.
    - Final-photo success displays that the listing is now draft and provides protected publish guidance after another image is persisted.
  </behavior>
  <action>Implement D-03, D-04, D-06, D-07, D-09, and D-11. Keep the manager dependent only on typed settings plus provider-neutral storage/lifecycle interfaces; it must not import Supabase, adapters, bucket names, keys, provider URLs, or cleanup credentials. Follow UI-SPEC for native multi-file accept, capacity, 4:3 tiles, Cover label, per-file ARIA progress/errors, labelled 44px remove targets, wrapping names, keyboard focus, and responsive grid. Use the lifecycle client for individual deletion and display its returned listing status; never assume status remains active after final-photo removal. Do not add reorder/manual-cover controls.</action>
  <verify><automated>npm test -- src/lib/listing-images.test.ts && npm run typecheck</automated></verify>
  <done>Owner photo operations are immediate and accessible, cannot disturb unsaved text, and accurately communicate final-image draft reversion.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Seller UI → storage/lifecycle contracts | UI has untrusted files and may be bypassed. |
| Owner draft query → edit UI | A private record must not be presented as published. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-11 | Tampering | UI status behavior | medium | mitigate | Server result controls displayed lifecycle status; UI does not write status directly. |
| T-06-12 | Repudiation | final-photo deletion | medium | mitigate | Explicit confirmation, returned draft state, retryable failure, and publish guidance. |
| T-06-13 | Information disclosure | draft presentation | high | mitigate | Owner-only edit query and no public draft route/card wiring. |
</threat_model>
<verification>Run focused tests/typecheck after each task; visual/device verification remains Plan 06.</verification>
<success_criteria>Sellers understand draft publication and immediate photo effects, while provider-specific storage remains absent from UI. Public draft object URLs are accepted MVP behavior and must not be presented as private delivery.</success_criteria>
<output>Create `.planning/phases/06-listing-image-management/06-04-SUMMARY.md` when done</output>

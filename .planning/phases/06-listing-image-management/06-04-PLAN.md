---
phase: 06-listing-image-management
plan: 04
type: execute
wave: 4
depends_on: [06-02, 06-03]
files_modified:
  - src/components/listing-card.tsx
  - src/components/listings-feed.tsx
  - src/components/my-listing-card.tsx
  - src/app/my-listings/page.tsx
  - src/lib/listing-image-cleanup-client.ts
  - src/lib/listing-images.test.ts
  - supabase/migrations/20260801010000_cut_over_listing_image_contract.sql
  - supabase/tests/listing-images-rls.sql
autonomous: false
requirements: [IMG-03, IMG-04, IMG-05]
must_haves:
  truths:
    - "Public cards use the first ordered image or the stock placeholder."
    - "Seller listing deletion invokes protected cleanup, permanently clears image resources, and updates local state only after the listing is soft-deleted."
    - "The legacy arbitrary image URL no longer competes with ordered image metadata."
  artifacts:
    - path: supabase/migrations/20260801010000_cut_over_listing_image_contract.sql
      provides: Approved legacy media contract removal
    - path: src/components/my-listing-card.tsx
      provides: Protected image cleanup and listing soft-deletion UX
  key_links:
    - from: src/components/my-listing-card.tsx
      to: supabase/functions/listing-image-cleanup/index.ts
      via: cleanup client invocation rather than direct DELETE
---

<objective>
Cut public/owner presentation over to ordered images and retire the legacy media contract under explicit approval.

Purpose: Complete D-02, D-03, and D-08 without allowing UI deletion to bypass protected server cleanup.
Output: Cover rendering, protected image-cleanup/soft-delete client wiring, cutover migration, and direct-mutation-denial regression.
</objective>

<execution_context>
@/Users/peakysmokin14/.codex/gsd-core/workflows/execute-plan.md
@/Users/peakysmokin14/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-listing-image-management/06-CONTEXT.md
@.planning/phases/06-listing-image-management/06-RESEARCH.md
@.planning/phases/06-listing-image-management/06-UI-SPEC.md
@.planning/phases/06-listing-image-management/06-03-SUMMARY.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Render the first ordered public cover and soft-delete one listing through cleanup</name>
  <files>src/components/listing-card.tsx, src/components/listings-feed.tsx, src/components/my-listing-card.tsx, src/app/my-listings/page.tsx, src/lib/listing-image-cleanup-client.ts, src/lib/listing-images.test.ts</files>
  <behavior>
    - A card selects the first ordered image and replaces a missing/broken cover with `/vehicle-placeholder.svg` in the same 4:3 frame.
    - Listing deletion invokes the cleanup function, disables owner controls while pending, retains visible state with retry guidance on error, and removes UI only after the returned listing status is `deleted`.
  </behavior>
  <action>Implement D-02, D-03, D-07, and D-08. Update feed and owner query consumers to consume `Listing.images` and the pure cover selector once; never construct provider URLs or render legacy arbitrary URLs. In `MyListingCard`, replace the direct owner database soft-delete mutation with the cleanup client function, state that photos are permanently removed and the listing is removed from sale, and only remove local UI after protected cleanup returns its soft-deleted listing result. Preserve stock fallback and accessible alt text from UI-SPEC.</action>
  <verify><automated>npm test -- src/lib/listing-images.test.ts && npm run typecheck</automated></verify>
  <done>Cards reliably show the ordered cover/placeholder and listing removal has a single browser route through protected image cleanup followed by the established soft-delete status transition.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Approve removal of the legacy `listings.image_url` data contract.</decision>
  <context>This is a one-way change under D-03 and D-08. Inspect hosted legacy values and confirm they are intentionally discarded; successful image-object and image-metadata removal is permanent while the listing row remains soft-deleted.</context>
  <options>
    <option id="approve-cutover"><name>Approve cutover</name><pros>Eliminates arbitrary external URLs and the competing media source.</pros><cons>Values and permanently deleted objects are not recoverable in-app.</cons></option>
    <option id="migrate-legacy-values"><name>Migrate legacy data first</name><pros>Preserves eligible assets.</pros><cons>Requires separately approved import/lifecycle design before cutover.</cons></option>
  </options>
  <resume-signal>Select `approve-cutover` or `migrate-legacy-values`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply the approved legacy contract cutover and deletion-policy regression</name>
  <precondition>The preceding checkpoint approved an exact legacy-data disposition and the Supabase CLI is authenticated/linked to project `qokumaemcqwkqhrxyolc`.</precondition>
  <reversibility rating="one-way">Removing the legacy column and permanently deleting image resources changes persistent data, per D-03 and D-08.</reversibility>
  <files>supabase/migrations/20260801010000_cut_over_listing_image_contract.sql, supabase/tests/listing-images-rls.sql</files>
  <action>After approval, write a forward-only migration that removes `listings.image_url` and its remaining constraints, preserves `listing_images` until protected cleanup removes it, and retains the browser DELETE/status-transition restrictions from Plan 02. Apply it to the linked project. Extend hosted regression to prove public object access before cleanup; owner/non-owner/anonymous direct DELETE denial for listing rows, image rows, and objects; direct owner `status = 'deleted'` update denial; and protected cleanup success removes associated image metadata, makes the public object unavailable after cache propagation, and leaves the listing row with status `deleted`. Use only Edge Function secrets for server authority and never add a browser service-role credential.</action>
  <verify><automated>supabase db push && supabase migration list && npm test -- src/lib/listing-images.test.ts && npm run typecheck</automated></verify>
  <done>The linked project no longer exposes the legacy media contract, denies direct client delete/status bypasses, and proves protected cleanup removes bytes and image metadata while retaining a soft-deleted listing row.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Card rendering → public CDN | Public media can be absent or broken. |
| Owner UI → cleanup function | A permanent request is authorized server-side. |
| Migration → hosted project | A one-way contract/policy change affects durable data. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-10 | Elevation | direct owner DELETE/status update | high | mitigate | Direct DELETE policies remain absent and deleted-status updates are restricted; hosted tests prove denial. |
| T-06-11 | Tampering | listing removal | high | mitigate | Browser invokes only protected cleanup and updates UI after successful image cleanup plus soft delete. |
| T-06-12 | Information disclosure | card media | medium | mitigate | Ordered application assets only with stock fallback. |
| T-06-13 | Repudiation | cutover | medium | mitigate | Explicit checkpoint, committed migration, parity, and hosted evidence. |
</threat_model>

<verification>Do not start final acceptance until migration parity, the deployed hosted harness proves direct delete/status denial and protected cleanup, and focused tests pass.</verification>
<success_criteria>There is exactly one public media source and exactly one authority for permanent image deletion and the listing deleted-status transition.</success_criteria>
<output>Create `.planning/phases/06-listing-image-management/06-04-SUMMARY.md` when done</output>

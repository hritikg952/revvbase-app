---
phase: 06-listing-image-management
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - src/lib/listing-images.test.ts
  - src/lib/listing-image-storage.test.ts
  - supabase/tests/listing-images-rls.sql
autonomous: false
requirements: [IMG-01, IMG-02, IMG-03, IMG-04, IMG-05]
must_haves:
  truths:
    - "Automated tests, typecheck, build, hosted RLS checks, and real browser checks verify the full image workflow."
    - "A seller can use supported photos, including HEIC where the approved decoder/device supports it, without breaking prior photos or unsaved listing edits."
    - "Desktop and mobile photo states emphasize Photos/Add photos and show correct cover, capacity, errors, focus, and placeholder fallback."
  artifacts:
    - path: src/lib/listing-images.test.ts
      provides: Final config/cover/validation regression cases
    - path: supabase/tests/listing-images-rls.sql
      provides: Final hosted authorization acceptance coverage
  key_links:
    - from: automated and hosted tests
      to: config, normalizer, service, storage policies, and UI
      via: phase release-gate commands and manual browser acceptance
---

<objective>
Run the final release gate and inspect the real owner/public experiences across browser and hosted Storage boundaries.

Purpose: Catch device, RLS, and responsive behavior that unit tests cannot prove.
Output: Green automated gate and a human acceptance decision backed by explicit browser steps.

## Artifacts this phase produces

- Final regression coverage for image domain/service and hosted RLS behavior.
- Phase summary with commands and acceptance evidence for later R2 migration work.
</objective>

<execution_context>
@/Users/peakysmokin14/.codex/gsd-core/workflows/execute-plan.md
@/Users/peakysmokin14/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-listing-image-management/06-CONTEXT.md
@.planning/phases/06-listing-image-management/06-VALIDATION.md
@.planning/phases/06-listing-image-management/06-UI-SPEC.md
@.planning/phases/06-listing-image-management/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the image-management automated and hosted release gate</name>
  <files>src/lib/listing-images.test.ts, src/lib/listing-image-storage.test.ts, supabase/tests/listing-images-rls.sql</files>
  <action>Complete any narrowly scoped missing regression cases exposed by the final integration, then run focused Vitest, the full test suite, typecheck, production build, migration parity, and linked hosted RLS test. Verify the settings deployment checklist is represented by tests/documented test fixtures: any future max count, byte, or stored-MIME change must update the versioned JSON and matching Supabase migration/bucket restriction together. Confirm the test suite does not contain provider details in listing UI/domain tests except through mocked contract boundaries.</action>
  <verify><automated>npm test && npm run typecheck && npm run build && supabase migration list && supabase test db --linked</automated></verify>
  <done>All release-gate commands pass, including current hosted migration parity and image RLS/cap checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: verify hosted browser image-management behavior</name>
  <what-built>Configured public, owner-managed listing photos with direct hosted Supabase delivery, independent edit-form controls, permanent cleanup, and automated regression coverage.</what-built>
  <how-to-verify>On desktop and at ≤600px, use a seller account to publish an image-less listing, open edit, then add JPEG, PNG, WebP, and HEIC where the approved device/browser supports it. Confirm each success becomes a 4:3 tile, first is Cover, fifth fills capacity, invalid/corrupt/oversized files show per-file errors with existing photos and unsaved text unchanged, and Remove photo confirms then updates Cover. Visit public feed signed out to verify a stable first-image cover and forced broken-image placeholder. With a different account, confirm upload/delete is denied. Delete the seller listing and confirm its public image URL no longer serves after normal cache propagation. Check keyboard focus, labelled controls, long filename wrapping, and that Photos/Add photos have stronger emphasis than capacity helper text.</how-to-verify>
  <resume-signal>Type `approved` or describe the failed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test credentials/browser sessions → linked Supabase | Acceptance probes operate against real authorization and public CDN behavior. |
| Responsive UI → seller actions | Accessibility and visible state must accurately represent destructive/failed operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-14 | Elevation | hosted owner checks | high | mitigate | Test second-account denial for upload/delete and SQL RLS assertions before human approval. |
| T-06-15 | Information disclosure | public CDN/cache | medium | mitigate | Confirm public assets are application-owned and permanent deletion stops delivery after cache propagation. |
| T-06-16 | Repudiation | cleanup verification | medium | mitigate | Record automated commands and human acceptance results in phase summary. |
| T-06-17 | Denial of service | browser processing | medium | mitigate | Exercise configured source/canonical limits and error isolation in actual target browsers. |
</threat_model>

<verification>
No final approval is valid until the automated command passes and the human browser/RLS checklist has an explicit outcome. Manual HEIC validation is required wherever the approved decoder and target device can run.
</verification>

<success_criteria>
- IMG-01 through IMG-05 have automated evidence, plus hosted/browser proof for controls that require real services or rendering.
- The product remains usable without images and with a broken image URL/object.
- Provider-neutrality, security enforcement, permanent cleanup, and UI hierarchy are all demonstrated rather than inferred.
</success_criteria>

<output>Create `.planning/phases/06-listing-image-management/06-04-SUMMARY.md` when done</output>

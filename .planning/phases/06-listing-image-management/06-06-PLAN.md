---
phase: 06-listing-image-management
plan: 06
type: execute
wave: 6
depends_on: [06-05]
files_modified:
  - src/lib/listing-images.test.ts
  - src/lib/listing-image-storage.test.ts
  - supabase/functions/listing-image-cleanup/lifecycle.test.ts
  - supabase/tests/listing-images-rls.sql
  - supabase/tests/hosted-listing-image-cleanup.ts
autonomous: false
requirements: [IMG-01, IMG-02, IMG-03, IMG-04, IMG-05]
must_haves:
  truths:
    - "Automated, hosted, and browser validation prove both values of the required-image setting, no-public-draft-record/metadata behavior, and accepted public draft object delivery."
    - "Final required-photo deletion cannot leave an active empty listing, and listing-wide cleanup remains object → metadata → deleted."
    - "HEIC, responsive accessibility, forged-key prevention, legacy cutover, direct mutation denial, and provider-neutral boundaries retain release gates."
  artifacts:
    - path: supabase/functions/listing-image-cleanup/lifecycle.test.ts
      provides: Publication and cleanup ordering regressions
    - path: supabase/tests/hosted-listing-image-cleanup.ts
      provides: Deployed two-policy-mode authorization/lifecycle regression
  key_links:
    - from: hosted lifecycle regression
      to: public listing/image queries
      via: Draft records and metadata are absent before publication, its public object URL is accepted MVP exposure, and the object URL fails only after successful protected cleanup
---

<objective>
Run final release validation for normalization, portable persistence, draft lifecycle, hosted Storage authorization, and seller UI.

Purpose: Prove the Phase 6 goal from user-visible outcomes and real Supabase boundaries, including D-11's two configuration modes.
Output: Green release evidence and human browser acceptance record.
</objective>

<execution_context>
@/Users/peakysmokin14/.codex/gsd-core/workflows/execute-plan.md
@/Users/peakysmokin14/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-listing-image-management/06-VALIDATION.md
@.planning/phases/06-listing-image-management/06-UI-SPEC.md
@.planning/phases/06-listing-image-management/06-05-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Run final-only release and deployed two-mode lifecycle gates</name>
  <files>src/lib/listing-images.test.ts, src/lib/listing-image-storage.test.ts, supabase/functions/listing-image-cleanup/lifecycle.test.ts, supabase/tests/listing-images-rls.sql, supabase/tests/hosted-listing-image-cleanup.ts</files>
  <action>Fill only gaps revealed by final integration, then run the final-only release suite. The hosted harness must reset/use the migration-owned policy mirror for both values and assert: draft-only insert; owner draft visibility; anonymous and different-user public draft record/metadata absence; public object delivery for a draft as accepted MVP behavior; `required=false` zero-image publish success; `required=true` zero-image publish rejection followed by one-image publish success; forged other-owner/other-listing key rejection; direct metadata/object/listing DELETE and all direct status-transition denial; final-image deletion draft reversion before object/metadata cleanup; listing-wide object → metadata → deleted ordering; public object delivery before cleanup and unavailability after cache propagation. Also confirm the JSON/migration mirror checklist and the approved HEIC dependency gate are recorded. Do not add private draft staging or promotion in response to this validation.</action>
  <verify><automated>npm test && deno test supabase/functions/listing-image-cleanup/lifecycle.test.ts && npm run typecheck && npm run build && supabase migration list && deno run --allow-env --allow-net supabase/tests/hosted-listing-image-cleanup.ts</automated></verify>
  <done>Final automated and hosted evidence proves both configuration modes, no public draft record/metadata exposure, accepted public draft object delivery, provider-neutral upload/key binding, direct mutation denial, and protected cleanup ordering.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: verify hosted browser photo and draft behavior</name>
  <what-built>Config-driven public images, protected draft publication, final-photo safety, permanent cleanup, and full automated/hosted regression coverage.</what-built>
  <how-to-verify>At desktop and ≤600px, test both deployed settings values in separate configured runs. With false, create a no-photo listing and confirm it becomes active/public with placeholder. With true, create one and confirm it stays owner-visible draft and absent from all public browsing until a photo succeeds and publish completes. Confirm that an uploaded draft photo's stable public object URL is intentionally reachable if copied directly, while its draft listing and metadata remain unavailable to the second account and public feed. Add JPEG/PNG/WebP and HEIC where the approved device/browser supports it; verify fifth-photo capacity, per-file errors, unsaved-text preservation, Cover progression, keyboard labels, and fallback. In true mode remove the final active photo and confirm immediate draft/absence from public feed, then re-add/publish. Use a second account to check draft metadata/upload/direct-delete/direct-status-update denial. Delete a listing and confirm permanent object removal after normal cache propagation while the retained record is deleted.</how-to-verify>
  <resume-signal>Type `approved` or describe failed behavior.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Release test sessions → linked Supabase | Tests exercise RLS, Edge JWT, policy mirror, and CDN behavior. |
| Browser seller UI → permanent lifecycle actions | Visible lifecycle state must match protected outcomes. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-17 | Elevation | hosted lifecycle authorization | high | mitigate | Two-mode owner/non-owner/forged-key/direct-mutation regression. |
| T-06-18 | Information disclosure | draft records and metadata | high | mitigate | Automated active-only query and manual cross-account no-draft-record/metadata checks. Public draft object delivery is accepted MVP scope; private staging/promotion is deferred. |
| T-06-19 | Tampering | required-image final deletion | high | mitigate | Unit, hosted, and browser assertions for draft reversion before deletion. |
| T-06-20 | Repudiation | release proof | medium | mitigate | Record commands and checkpoint result in phase summary. |
</threat_model>
<verification>No final approval is valid without green release commands, deployed two-mode harness evidence, and approved HEIC/responsive browser confirmation.</verification>
<success_criteria>IMG-01 through IMG-05 have unit, hosted, and browser evidence for secure public photos plus configuration-driven draft publication.</success_criteria>
<output>Create `.planning/phases/06-listing-image-management/06-06-SUMMARY.md` when done</output>

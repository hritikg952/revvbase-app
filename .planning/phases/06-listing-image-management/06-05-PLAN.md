---
phase: 06-listing-image-management
plan: 05
type: execute
wave: 5
depends_on: [06-02, 06-03, 06-04]
files_modified:
  - src/components/listing-card.tsx
  - src/components/listings-feed.tsx
  - src/components/my-listing-card.tsx
  - src/app/my-listings/page.tsx
  - src/lib/listing-images.test.ts
  - supabase/migrations/20260801010000_cut_over_listing_image_contract.sql
  - supabase/tests/listing-images-rls.sql
autonomous: false
requirements: [IMG-03, IMG-04, IMG-05]
must_haves:
  truths:
    - "Visitors receive only active listing cards and see first ordered cover or placeholder."
    - "Owners see active and draft listings in management, while listing deletion invokes protected lifecycle cleanup only."
    - "Legacy arbitrary image URLs no longer compete with ordered metadata after explicit approval."
  artifacts:
    - path: supabase/migrations/20260801010000_cut_over_listing_image_contract.sql
      provides: Approved legacy image URL contract removal
    - path: src/components/my-listing-card.tsx
      provides: Protected listing cleanup interaction
  key_links:
    - from: src/components/my-listing-card.tsx
      to: src/lib/listing-image-lifecycle-client.ts
      via: Protected listing-delete action rather than direct database mutation
---

<objective>
Cut presentation and owner listing management over to ordered images, drafts, and protected lifecycle actions.

Purpose: Complete D-02, D-03, D-08, and D-11 without public draft record/metadata leaks or legacy media ambiguity; public-bucket draft object URLs remain accepted MVP scope.
Output: Active-only card/feed rendering, draft-aware owner collection, cleanup wiring, and approved cutover migration.
</objective>

<execution_context>
@/Users/peakysmokin14/.codex/gsd-core/workflows/execute-plan.md
@/Users/peakysmokin14/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-listing-image-management/06-CONTEXT.md
@.planning/phases/06-listing-image-management/06-UI-SPEC.md
@.planning/phases/06-listing-image-management/06-04-SUMMARY.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Render an active ordered cover while owner management includes drafts</name>
  <files>src/components/listing-card.tsx, src/components/listings-feed.tsx, src/components/my-listing-card.tsx, src/app/my-listings/page.tsx, src/lib/listing-images.test.ts</files>
  <behavior>
    - Public feed query/cards receive active listings only and pick first ordered photo or the existing placeholder in the same frame.
    - Owner management query includes draft/active records but excludes deleted; a draft has visible owner-only status and edit path.
    - Listing deletion invokes lifecycle cleanup, keeps UI on failure, and removes UI only after the returned status is deleted.
  </behavior>
  <action>Implement D-02, D-03, D-07, D-08, and D-11. Map ordered relations once at query boundaries. Keep public feed queries constrained to active (as a defense-in-depth counterpart to RLS), and never construct provider URLs or render `image_url`. Update My Listings to fetch own draft and active statuses, visibly label drafts without a public-link affordance, and replace direct soft-delete mutation with the protected lifecycle client. Do not introduce private staging or promotion: draft object URLs remain public-bucket behavior but draft records and metadata stay out of public application consumers. Preserve stock fallback, accessible alt text, confirmation, disabled pending controls, and retry guidance.</action>
  <verify><automated>npm test -- src/lib/listing-images.test.ts && npm run typecheck</automated></verify>
  <done>Public cards cannot render drafts; owner management handles drafts intentionally; listing deletion is protected and correctly reflected only after success.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Approve removal of the legacy `listings.image_url` contract.</decision>
  <context>D-03 makes ordered application-owned metadata the sole media source. Inspect linked-project legacy values and confirm their intentional discard before the forward-only column removal.</context>
  <options><option id="approve-cutover"><name>Approve cutover</name><pros>Removes arbitrary external URLs and a competing media source.</pros><cons>Legacy values cannot be restored in-app.</cons></option><option id="migrate-legacy-values"><name>Migrate legacy data first</name><pros>Preserves eligible historical assets.</pros><cons>Requires an approved import design before this migration.</cons></option></options>
  <resume-signal>Select `approve-cutover` or `migrate-legacy-values`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply the approved legacy-media cutover and visibility regression</name>
  <precondition>The preceding checkpoint selected a legacy-data disposition and the linked Supabase project is available.</precondition>
  <reversibility rating="one-way">Removing the legacy media column permanently changes the published listing contract under D-03.</reversibility>
  <files>supabase/migrations/20260801010000_cut_over_listing_image_contract.sql, supabase/tests/listing-images-rls.sql</files>
  <action>After approval, write/apply a forward-only migration that removes `listings.image_url` and its constraint without weakening the draft/active/deleted constraint, active-only public policy, owner draft reads, or direct browser lifecycle-mutation denial. Extend deployed-policy regression to assert no public draft record/metadata selects or relations, owner draft select, active-only card query behavior, accepted public-bucket draft object delivery, and the existing protected cleanup guarantees. Do not introduce browser service-role credentials, direct Storage SQL mutation, or private draft staging/promotion.</action>
  <verify><automated>supabase db push && supabase migration list && npm test -- src/lib/listing-images.test.ts && npm run typecheck</automated></verify>
  <done>The only application media contract is ordered image metadata, and deployed policies continue to make drafts owner-only and destructive lifecycle actions protected.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Public feed → relation mapping | Data/query mistakes could reveal drafts or stale legacy URLs. |
| Owner card → lifecycle endpoint | Permanent deletion remains a privileged operation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-14 | Information disclosure | public feed/draft record relation | high | mitigate | Active predicate in query plus RLS regression for draft record/metadata absence. Public draft object delivery is accepted MVP scope; private staging/promotion is deferred. |
| T-06-15 | Elevation | owner listing deletion | high | mitigate | Lifecycle client only; direct mutation denial remains hosted-tested. |
| T-06-16 | Repudiation | legacy cutover | medium | mitigate | Blocking decision, forward migration, and hosted policy evidence. |
</threat_model>
<verification>Do not start final acceptance until cutover is approved, migration parity passes, and draft visibility tests remain green.</verification>
<success_criteria>Public and owner consumers express the correct lifecycle state, with exactly one media source and protected destructive authority.</success_criteria>
<output>Create `.planning/phases/06-listing-image-management/06-05-SUMMARY.md` when done</output>

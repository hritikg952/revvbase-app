---
phase: 06-listing-image-management
plan: 05
type: execute
wave: 5
depends_on: [06-04]
files_modified:
  - src/lib/listing-images.test.ts
  - src/lib/listing-image-storage.test.ts
  - supabase/functions/listing-image-cleanup/cleanup.test.ts
  - supabase/tests/listing-images-rls.sql
  - supabase/tests/hosted-listing-image-cleanup.ts
autonomous: false
requirements: [IMG-01, IMG-02, IMG-03, IMG-04, IMG-05]
must_haves:
  truths:
    - "Automated, hosted, and browser checks prove the complete image workflow."
    - "Hosted evidence proves direct deletion and direct deleted-status updates are denied; protected cleanup removes image bytes/metadata then soft-deletes the listing."
    - "HEIC and responsive accessibility checks retain their explicit human gates."
  artifacts:
    - path: supabase/functions/listing-image-cleanup/cleanup.test.ts
      provides: Cleanup-order unit regression
    - path: supabase/tests/hosted-listing-image-cleanup.ts
      provides: Executable deployed Edge Function authorization and cleanup regression
  key_links:
    - from: hosted cleanup regression
      to: public object URL
      via: object becomes unavailable only after protected cleanup succeeds and the returned listing has deleted status
---

<objective>
Run final release validation across the normalizer, protected cleanup, hosted Storage, and responsive UI.

Purpose: Demonstrate every Phase 6 outcome rather than inferring cross-service safety from SQL alone.
Output: Green release commands and human hosted/browser acceptance evidence.
</objective>

<execution_context>
@/Users/peakysmokin14/.codex/gsd-core/workflows/execute-plan.md
@/Users/peakysmokin14/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-listing-image-management/06-VALIDATION.md
@.planning/phases/06-listing-image-management/06-UI-SPEC.md
@.planning/phases/06-listing-image-management/06-04-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Run final-only release and deployed protected-cleanup gates</name>
  <files>src/lib/listing-images.test.ts, src/lib/listing-image-storage.test.ts, supabase/functions/listing-image-cleanup/cleanup.test.ts, supabase/tests/listing-images-rls.sql, supabase/tests/hosted-listing-image-cleanup.ts</files>
  <action>Fill only gaps found by final integration. Treat this task as the final-only full release gate; use the focused per-task commands in Plans 01–04 during implementation. Run normalizer/contract tests, Edge Function mocked-sequencing tests, the full test suite, typecheck, production build, and migration parity. Run `deno run --allow-env --allow-net supabase/tests/hosted-listing-image-cleanup.ts` against the deployed function using the dedicated non-privileged owner/non-owner test sessions. Its assertions must cover owner upload/registration, public URL before cleanup, forged other-owner and other-listing key rejection, non-owner denial, direct owner DELETE denial, direct owner deleted-status-update denial, Edge Function invocation, object unavailability after cache propagation, image metadata removal, and the retained listing row with `status = 'deleted'`. Check the paired JSON/migration release checklist whenever maximum count, MIME, or canonical byte rules change.</action>
  <verify><automated>npm test && deno test supabase/functions/listing-image-cleanup/cleanup.test.ts && npm run typecheck && npm run build && supabase migration list && deno run --allow-env --allow-net supabase/tests/hosted-listing-image-cleanup.ts</automated></verify>
  <done>Final automated and hosted evidence covers provider-neutral upload, forged-key protection, direct delete/status denial, object-before-metadata-before-soft-delete cleanup, public delivery, and config mirror discipline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: verify hosted browser image-management behavior</name>
  <what-built>Config-driven public photos, protected permanent cleanup, and final automated/hosted regression coverage.</what-built>
  <how-to-verify>At desktop and ≤600px, create an image-less listing, edit it, add JPEG/PNG/WebP and HEIC where the approved device/browser supports it, and confirm fifth-photo capacity, per-file failures, unsaved-text preservation, Cover progression, labelled keyboard controls, and stock fallback. Use a second account to verify upload/direct-delete/direct-deleted-status-update denial. Remove one photo and confirm only that object and metadata disappear. Remove a listing through the UI and confirm its public image URL no longer serves after normal cache propagation while the listing remains absent from public browsing because its persisted status is deleted.</how-to-verify>
  <resume-signal>Type `approved` or describe failed behavior.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Test sessions → linked Supabase | Release checks exercise real authorization and CDN behavior. |
| Responsive seller UI → permanent action | Visible status must accurately reflect cleanup outcomes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-14 | Elevation | hosted cleanup authorization | high | mitigate | Owner/non-owner, forged-key, direct-delete, and direct-status-update regressions. |
| T-06-15 | Information disclosure | public CDN/cache | medium | mitigate | Verify only canonical public asset and post-cleanup unavailability. |
| T-06-16 | Repudiation | cleanup proof | medium | mitigate | Record command results and human outcome in phase summary. |
</threat_model>

<verification>No final approval is valid without green final-only release commands, deployed hosted cleanup evidence, and the retained HEIC/responsive browser checkpoint.</verification>
<success_criteria>IMG-01 through IMG-05 have automated evidence plus hosted/browser proof for real service and visual behavior.</success_criteria>
<output>Create `.planning/phases/06-listing-image-management/06-05-SUMMARY.md` when done</output>

---
phase: 06-listing-image-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/app-settings.json
  - src/lib/listing-images.ts
  - src/lib/image-normalizer.client.ts
  - src/lib/listing-images.test.ts
  - package.json
  - package-lock.json
autonomous: false
requirements: [IMG-01, IMG-02]
must_haves:
  truths:
    - "Static JSON controls required-image lifecycle UX, capacity, source acceptance, source safety, canonical output size, and display settings."
    - "A supported native photo becomes a verified WebP at or below 1 MB; invalid input changes no prior image state."
    - "HEIC/HEIF reaches the same canonical path only through a reviewed decoder."
  artifacts:
    - path: src/config/app-settings.json
      provides: Extensible versioned application settings
    - path: src/lib/image-normalizer.client.ts
      provides: Client-only canonical image preparation
  key_links:
    - from: src/config/app-settings.json
      to: src/lib/listing-images.ts
      via: Typed settings parser used by validation and UI
---

<objective>
Build and prove the config-driven browser normalization boundary before storage or UI integration.

Purpose: Enforce D-04 through D-06 and D-09/D-10 without duplicating image limits in components.
Output: Versioned settings, pure rule helpers, canonical normalizer, and focused tests.
</objective>

<execution_context>
@/Users/peakysmokin14/.codex/gsd-core/workflows/execute-plan.md
@/Users/peakysmokin14/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-listing-image-management/06-CONTEXT.md
@.planning/phases/06-listing-image-management/06-RESEARCH.md
@.planning/phases/06-listing-image-management/06-VALIDATION.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Normalize one native-decodable photo under JSON rules</name>
  <files>src/config/app-settings.json, src/lib/listing-images.ts, src/lib/image-normalizer.client.ts, src/lib/listing-images.test.ts</files>
  <behavior>
    - A valid JPEG, PNG, or WebP produces WebP within configured dimensions and 1,048,576 output bytes.
    - Corrupt, unsupported, over-source-safety, and cannot-fit inputs return a file-specific result and cause no persistence call.
    - Required-image lifecycle copy, five-photo capacity, MIME accept value, output cap, and display values come from typed settings rather than component literals.
  </behavior>
  <action>Implement D-04, D-05, D-06, D-09, D-10, and D-11. Create `app-settings.json` with `schemaVersion: 1`, `images.required`, `maxPerListing`, allowed JPEG/PNG/WebP/HEIC/HEIF sources, source safety ceiling, canonical WebP MIME/byte cap, long-edge, quality bounds, and display dimensions. Parse and validate it in pure typed helpers, including exact draft/publication helper copy for each `images.required` value; use Canvas decode, resize, iterative WebP encoding, and actual MIME/byte checks in the client-only normalizer. Retain no original and never rely on filename extension/MIME alone. Start with failing Vitest cases, then implement the verified one-file path.</action>
  <verify><automated>npm test -- src/lib/listing-images.test.ts && npm run typecheck</automated></verify>
  <done>Native-decodable files produce only configured canonical output or precise non-mutating per-file errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: approve the HEIC/HEIF decoder dependency</name>
  <what-built>The native canonical path and isolated HEIC dynamic-import seam are ready; no unreviewed package has been installed.</what-built>
  <how-to-verify>Review a candidate package and exact version on npm and its official repository for maintainer activity, license, package scripts, browser/device compatibility, bundle footprint, and TypeScript support. Approve one exact package/version or request another candidate.</how-to-verify>
  <resume-signal>Provide the approved package and version, or name a different reviewed candidate.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route reviewed HEIC/HEIF files through the canonical normalizer</name>
  <precondition>A human approved one exact HEIC/HEIF decoder package and version at the preceding checkpoint.</precondition>
  <files>package.json, package-lock.json, src/lib/image-normalizer.client.ts, src/lib/listing-images.test.ts</files>
  <behavior>
    - HEIC/HEIF dynamically decodes into the same bitmap-to-WebP pipeline.
    - Decoder load/decode/output failures return configured file-specific errors without upload or prior-state mutation.
  </behavior>
  <action>Install only the checkpoint-approved package/version. Dynamically import it solely for HEIC/HEIF in the normalizer, pass decoded pixels to the existing canonical pipeline, and add regression cases for decoder and output-limit failures. Keep decoder details out of UI, storage contracts, and settings consumers, per D-05.</action>
  <verify><automated>npm test -- src/lib/listing-images.test.ts && npm run typecheck</automated></verify>
  <done>Approved HEIC/HEIF conversion is isolated, test-covered, and produces the same canonical asset contract as native formats.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser file → normalizer | Untrusted bytes, claimed MIME, dimensions, and metadata enter processing. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Tampering | normalizer | high | mitigate | Decode and re-encode, then verify canonical MIME/bytes and settings limits. |
| T-06-02 | Denial of service | source processing | medium | mitigate | Configured source safety ceiling and bounded resize/quality loop. |
| T-06-SC | Tampering | npm decoder install | high | mitigate | Blocking human legitimacy gate before installation. |
</threat_model>

<verification>Run focused tests and typecheck after each task; retain the real-device HEIC smoke check for Plan 05.</verification>
<success_criteria>Settings are the single browser source of image rules, and canonical output is validated before any storage operation.</success_criteria>
<output>Create `.planning/phases/06-listing-image-management/06-01-SUMMARY.md` when done</output>

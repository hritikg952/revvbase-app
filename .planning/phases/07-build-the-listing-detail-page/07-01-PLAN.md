---
phase: 07-build-the-listing-detail-page
plan: 01
type: execute
wave: 1
depends_on: [06-06]
files_modified:
  - src/app/listings/[id]/page.tsx
  - src/components/listing-detail.tsx
  - src/components/listing-media-gallery.tsx
  - src/lib/listing-detail-images.ts
  - src/lib/listing-detail-images.test.ts
  - src/lib/listing-detail-media.ts
  - src/lib/listing-detail-media.test.ts
  - src/app/globals.css
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "A public visitor can open an active listing at /listings/[id] and see a photo-led vehicle summary with its exact city, title, price, and facts."
    - "Detail media stays inside the finalized Phase 6 public image boundary and safely presents a placeholder, static photo, or usable manual carousel by ordered image count."
  artifacts:
    - path: src/app/listings/[id]/page.tsx
      provides: Dynamic public listing-detail route
    - path: src/lib/listing-detail-images.ts
      provides: Phase 7 consumer adapter for finalized ordered public image contract
    - path: src/components/listing-media-gallery.tsx
      provides: Accessible 0/1/many media rendering
  key_links:
    - from: src/app/listings/[id]/page.tsx
      to: src/lib/listing-detail-images.ts
      via: Finalized Phase 6 public image consumer surface rather than storage internals
    - from: src/components/listing-detail.tsx
      to: src/components/listing-media-gallery.tsx
      via: Ordered public image descriptors and stock fallback
---

<objective>
Deliver the production-quality public-detail tracer: one active listing reaches a responsive, photo-led route through the existing browser Supabase/RLS boundary and finalized Phase 6 image consumer contract.

Purpose: Establish the actual public read, media, and responsive composition before expanding owner/auth and feed-continuity behavior.
Output: Dynamic detail route, Phase 6-compatible image adapter, gallery behavior, focused tests, and detail-page styling.
</objective>

<execution_context>
@/Users/peakysmokin14/.codex/gsd-core/workflows/execute-plan.md
@/Users/peakysmokin14/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-build-the-listing-detail-page/07-CONTEXT.md
@.planning/phases/07-build-the-listing-detail-page/07-RESEARCH.md
@.planning/phases/06-listing-image-management/06-06-SUMMARY.md
@src/components/listings-feed.tsx
@src/components/listing-card.tsx
@src/lib/storage/listing-image-storage.ts
@src/lib/storage/supabase-listing-images.ts
@src/lib/listings.ts
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Open one active listing in the photo-led public detail route</name>
  <precondition>Phase 6 Plan 06-06 is complete; its release SUMMARY and finalized stable public image-consumer type surface are present, and the active-listing/image RLS contract remains deployed.</precondition>
  <files>src/app/listings/[id]/page.tsx, src/components/listing-detail.tsx, src/lib/listing-detail-images.ts, src/lib/listing-detail-images.test.ts</files>
  <read_first>
    - .planning/phases/06-listing-image-management/06-06-SUMMARY.md and the finalized src/lib/storage files: use their actual exported consumer surface; they are a read-only dependency.
    - src/components/listings-feed.tsx, src/components/listing-card.tsx, src/lib/listings.ts, src/lib/database.types.ts, and src/app/listings/[id]/edit/page.tsx: preserve browser-client, active-query, loading, neutral-state, and formatting conventions.
  </read_first>
  <behavior>
    - An active listing ID loads one active row through the browser Supabase client; missing, deleted, draft, malformed, or RLS-hidden IDs receive the same neutral unavailable state.
    - The detail heading is exactly Make Model · Year, price uses formatPrice, and city renders the stored listing value without enrichment (D-02, D-06).
    - The desktop view is an asymmetric media-and-summary hero; on narrow screens media precedes the summary at full available width (D-01).
    - The Phase 7 adapter accepts only ordered public image records, returns safe display descriptors, and treats unavailable/malformed image results as an empty media set without exposing a storage key.
  </behavior>
  <action>Create the client-facing dynamic route and `ListingDetail` composition as the thin end-to-end path for D-01, D-02, and D-06. Query `listings` by route ID and `status = active` through `getSupabaseBrowserClient`; retain RLS as authorization and present one neutral unavailable state rather than distinguishing absence reasons. Before code changes, inspect the final Phase 6 summary and actual types. Add a Phase-7-local image consumer adapter that feature-detects the finalized public consumer capability by requiring a callable ordered-list operation and records with a public URL plus numeric position; prefer a finalized Phase 6 public helper when it exists, otherwise use the existing provider-neutral `ListingImageStorage.list` interface at this one boundary. Sort and normalize only the adapter output; guard failures and let the UI use the stock illustration. Do not modify Phase 6, query `listing_images` in a component, construct provider URLs, read storage keys into display code, or add credentials. Write focused tests first for ordered valid records, malformed records, adapter failures, and no-image fallback, then implement the route/composition until they pass.</action>
  <verify>
    <automated>npm test -- src/lib/listing-detail-images.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>An active public route renders the approved identity, price, exact supplied location, facts, and first usable media through the finalized storage-neutral contract; all unavailable states remain neutral and safe.</acceptance_criteria>
  <done>The public route is a working tracer from route parameter through active RLS-scoped data and image adapter to responsive detail output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render media by ordered image count with manual keyboard controls</name>
  <files>src/components/listing-media-gallery.tsx, src/lib/listing-detail-media.ts, src/lib/listing-detail-media.test.ts, src/app/globals.css</files>
  <read_first>
    - src/lib/listing-detail-images.ts and src/components/listing-detail.tsx from Task 1: consume only normalized display descriptors.
    - src/components/listing-card.tsx and src/app/globals.css: retain the stock-image fallback and warm-paper/green-ink/orange-accent design language.
    - .planning/phases/07-build-the-listing-detail-page/07-CONTEXT.md: D-01 and D-05 are locked layout/media rules.
  </read_first>
  <behavior>
    - Zero images render the established stock illustration; one image is static hero media with no gallery controls (D-05).
    - Two or more ordered images render a non-rotating carousel with visible previous/next buttons and numbered indicator buttons, and previous/next wrap at either end (D-05).
    - Native button semantics make every control Tab/Enter/Space operable, labels announce the selected photo and count, and failed media falls back safely.
  </behavior>
  <action>Extract pure image-count/index helpers and write their failing tests before the gallery implementation. Build `ListingMediaGallery` around native buttons and a local selected index only for a multi-image set. Implement D-05 exactly: stock fallback for none, ordinary static media for one, and arrows plus indicators for two-or-more; do not add thumbnails, auto-rotation, zoom, lightbox, manual cover selection, or reordering. Use descriptive `alt` text with position/count for seller media, an accessible carousel label only for the multi-image branch, and an image-load fallback. Add detail-specific CSS that realizes D-01's asymmetric desktop hero and full-width-first mobile media, retains reduced-motion compatibility, and keeps controls visibly focused and touch sized.</action>
  <verify>
    <automated>npm test -- src/lib/listing-detail-media.test.ts &amp;&amp; npm run typecheck</automated>
    <human-check>In a browser, use Tab then Enter/Space on carousel arrows and indicators for a listing with at least two ordered photos; confirm each changes the displayed photo, wrapping works, and a one-photo listing exposes no controls.</human-check>
  </verify>
  <acceptance_criteria>Media follows the required 0/1/2+ branches, has no thumbnails, and multi-photo controls are visible and keyboard-operable.</acceptance_criteria>
  <done>The tracer now has its full approved media presentation and a deterministic regression suite for count/index behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Route parameter and public client → listing query | Untrusted ID must not identify draft/deleted visibility states. |
| Finalized Phase 6 image contract → detail UI | Image metadata/provider changes must not leak keys or break placeholder safety. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01 | Information disclosure | public detail listing read | high | mitigate | Query active records only, preserve RLS, and render one neutral unavailable state. |
| T-07-02 | Tampering | image-consumer adapter | medium | mitigate | Validate callable public contract and normalized records; do not expose provider paths/keys or bypass the adapter. |
| T-07-03 | Denial of service | malformed image response | low | mitigate | Guard adapter failures and retain the stock image fallback. |
</threat_model>

<verification>
Run the focused adapter/media tests, typecheck, and production build. The browser keyboard assertion is required before marking the media task accepted.
</verification>

<success_criteria>
One public active listing can be opened as a responsive, photo-led detail route with correct vehicle identity and safe ordered-image behavior, without changing Phase 6 or any authorization boundary.
</success_criteria>

## Artifacts this phase produces

- `src/app/listings/[id]/page.tsx` — public dynamic route.
- `src/components/listing-detail.tsx` and `src/components/listing-media-gallery.tsx` — photo-led detail composition and media branches.
- `src/lib/listing-detail-images.ts` and `src/lib/listing-detail-media.ts` — Phase-7-only image/selection boundaries with unit tests.

<output>
Create `.planning/phases/07-build-the-listing-detail-page/07-01-SUMMARY.md` when done.
</output>

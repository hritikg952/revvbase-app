---
phase: 07-build-the-listing-detail-page
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - src/components/listing-card.tsx
  - src/components/listings-feed.tsx
  - src/components/listing-detail.tsx
  - src/lib/listing-return.ts
  - src/lib/listing-return.test.ts
  - src/app/globals.css
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "Only a short-lived, nonce-proven feed visit can use browser Back or in-app Back, and that claimed visit returns to the loaded feed with its opened card visible."
    - "The full phase passes automated type/test/build checks and human keyboard, mobile, and history-continuity acceptance checks."
  artifacts:
    - path: src/components/listings-feed.tsx
      provides: Post-load restoration of a claimed nonce-bound feed visit
    - path: src/components/listing-card.tsx
      provides: Accessible card-to-detail navigation and nonce-bound provenance capture
    - path: src/lib/listing-return.test.ts
      provides: Deterministic nonce creation, exact claim, expiry, and one-shot consumption behavior
  key_links:
    - from: src/components/listing-card.tsx
      to: src/app/listings/[id]/page.tsx
      via: Internal detail navigation carrying the exact feed-issued provenance nonce
    - from: src/components/listings-feed.tsx
      to: src/lib/listing-return.ts
      via: Consume-once restoration only after a claimed record's target card renders
---

<objective>
Connect public cards to the completed detail page and preserve feed position across the real history-return path, then run the phase release gates.

Purpose: Make detail navigation feel continuous rather than ejecting visitors back to the feed top.
Output: Accessible card links, fail-closed nonce-bound return protocol, post-load restoration logic, regression tests, and complete acceptance evidence.
</objective>

<execution_context>
@/Users/peakysmokin14/.codex/gsd-core/workflows/execute-plan.md
@/Users/peakysmokin14/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-build-the-listing-detail-page/07-CONTEXT.md
@.planning/phases/07-build-the-listing-detail-page/07-RESEARCH.md
@.planning/phases/07-build-the-listing-detail-page/07-01-SUMMARY.md
@.planning/phases/07-build-the-listing-detail-page/07-02-SUMMARY.md
@src/components/listing-card.tsx
@src/components/listings-feed.tsx
@src/lib/listing-return.ts
@src/app/globals.css
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce nonce-bound one-shot provenance for feed-detail return</name>
  <files>src/components/listing-card.tsx, src/components/listings-feed.tsx, src/components/listing-detail.tsx, src/lib/listing-return.ts, src/lib/listing-return.test.ts, src/app/globals.css</files>
  <read_first>
    - .planning/phases/06-listing-image-management/06-06-SUMMARY.md plus src/components/listings-feed.tsx and src/components/listing-card.tsx: Phase 6 is complete/read-only; preserve its loading, empty, error, cover-fallback, and public active-query behavior.
    - src/app/listings/[id]/page.tsx and src/components/listing-detail.tsx: consume the completed route and use its in-app Back affordance only through a claim capability, never from storage presence.
    - src/lib/listing-return.ts and 07-02-SUMMARY.md: extend the validated pathname helper rather than duplicating storage/URL parsing.
    - .planning/phases/07-build-the-listing-detail-page/07-CONTEXT.md: D-11 is a locked user-continuity outcome.
  </read_first>
  <behavior>
    - A card activation generates a cryptographically random nonce, stores one namespaced short-lived `{ version, listingId, scrollY, nonce, createdAt, expiresAt, state: pending }` session record, and navigates to `/listings/{id}?fromFeed={nonce}` (D-11).
    - The detail route claims the record only when the pathname ID, opaque `fromFeed` value, expiry, pending state, and record shape exactly match; the successful claim is one-shot and returns an in-memory back capability bound to that visit (D-11).
    - Only the component holding that successful capability may call `router.back()`; direct, missing, malformed, expired, listing-mismatched, nonce-mismatched, or reused attempts use the normal listings fallback and cannot invoke history Back (D-11).
    - On history return, the feed waits for loading to finish and the matching card anchor to render, then restores and removes only the claimed matching record. Direct feed entry, malformed state, missing target, or failed public load does not force an unrelated scroll.
  </behavior>
  <action>Write failing unit tests first for record creation, nonce opacity, finite-offset validation, exact listing-ID/nonce matching, expiry with an injected clock, pending-to-claimed one-shot transition, rejected second claim, claimed-only feed consumption, and every fail-closed fallback case. Define the namespaced record schema and TTL in `listing-return.ts`; create one nonce with `crypto.randomUUID()` immediately before an accessible card link navigates to the internal detail URL carrying that exact nonce. Parse the detail query defensively and expose a claim function that validates the full record then transitions it from pending to claimed exactly once; return a local capability rather than a broadly reusable storage flag. `ListingDetail` may call `router.back()` only while holding that successful capability. All claim failures—including direct URL entry—must render/use the ordinary listings fallback without history navigation. Add a stable listing-ID anchor to the linked card. When the feed remounts, inspect only a claimed, unexpired, well-formed record; after its existing async load completes and the matching anchor is in the DOM, restore the card into view, then remove the record. If the target never renders or the public load fails, clear the unusable record without scrolling. Preserve Phase 6 public image consumption, active-only query, and existing feed states; do not modify Phase 6, add global state, or create a custom history layer.</action>
  <verify>
    <automated>npm test -- src/lib/listing-return.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Only an exact, unexpired, one-time feed provenance claim enables browser/in-app history Back, and that claimed path leaves the opened card visible after the feed has rendered.</acceptance_criteria>
  <done>The completed listing-detail route has a fail-closed, nonce-bound, one-shot feed-to-detail-to-feed protocol rather than storage-presence history behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Run verification-only cross-route release and browser acceptance gates</name>
  <files>None — verification-only task; application and test files are not changed here.</files>
  <read_first>
    - 07-01-SUMMARY.md and 07-02-SUMMARY.md: execute the completed tracer and its expansions as one user journey.
    - .planning/phases/07-build-the-listing-detail-page/07-CONTEXT.md: verify every D-01 through D-11; do not turn deferred items into acceptance work.
  </read_first>
  <action>Run the full deterministic suite and the browser checks using realistic active listings with zero, one, and multiple ordered Phase 6 photos. This is verification-only: do not change application code, tests, configuration, migrations, or Phase 6 artifacts. Record command outcomes and any environment limitation in the phase SUMMARY; if a gate fails, stop and report the failing gate for a separately scoped correction plan.</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test &amp;&amp; npm run build</automated>
    <human-check>
      1. Desktop: open an active listing and confirm the asymmetric photo-led hero, exact Make Model · Year heading, stored location, facts, price, decorative non-actionable heart, and no similar-listings area.
      2. Gallery: verify placeholder for no photos, no controls for one photo, and visible wrapping arrows/indicators with Tab, Enter, and Space support for two or more photos.
      3. Content/actions: expand and collapse a long description; as owner see Edit listing, as a different signed-in visitor see Make an offer, and signed out round-trip through sign-in to the originating listing. Verify no offer/message is created.
      4. Mobile at 600px or narrower: media appears before summary and the sticky area contains only the current primary CTA, with no price repeat.
      5. Continuity: scroll the public feed so a target card is away from the top, open it, and confirm its URL carries a feed-issued opaque nonce. Return once with browser Back and once with the claimed in-app Back affordance; each must reveal the target after the feed renders. In separate trials, directly open a detail URL, remove or alter the nonce, use a nonce for another listing, wait past its TTL, and reuse a claimed nonce; every case must use the listings fallback without calling history Back or forcing a feed scroll.
    </human-check>
  </verify>
  <acceptance_criteria>Automated checks are green and the keyboard, responsive mobile, signed-in/signed-out, browser-back, and in-app-back paths all satisfy their documented outcomes.</acceptance_criteria>
  <done>The phase has release evidence for the approved public detail experience and its real navigation continuity.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public feed click → browser session state | Stored return state is untrusted persisted client data. |
| Detail history action → feed restoration | Navigation must preserve browser history without an incorrect forced scroll. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-07 | Tampering | sessionStorage provenance record and route nonce | high | mitigate | Require versioned shape, finite offset, exact ID/nonce, expiry, and pending-to-claimed one-shot transition before history capability exists. |
| T-07-08 | Elevation of privilege | history Back capability | medium | mitigate | Keep the successful claim capability local to the rendered visit; all direct/reused/nonmatching visits use listings fallback. |
| T-07-10 | Denial of service | async feed restoration | low | mitigate | Restore only a claimed record after load/target readiness and make missing-target handling terminal-safe. |
| T-07-09 | Repudiation | phase behavior release | medium | mitigate | Full automated commands plus explicit keyboard/mobile/history browser checklist in the SUMMARY. |
</threat_model>

<verification>
The Phase 7 release gate is `npm run typecheck &amp;&amp; npm test &amp;&amp; npm run build` plus all five browser acceptance sequences in Task 2.
</verification>

<success_criteria>
The public feed and detail page form one accessible journey whose return path preserves the user’s context, and every approved decision has automated or browser evidence.
</success_criteria>

## Source coverage audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Responsive public photo-led listing-detail page | 07-01 public route/media tracer; 07-02 content/actions; 07-03 continuity/release gate |
| REQ | Formal Phase 7 requirement IDs are TBD in ROADMAP/REQUIREMENTS | `requirements: [TBD]` in every plan; D-01 through D-11 are treated as the executable acceptance contract |
| RESEARCH | Active-only public read, adapter ownership, media branches, safe auth return, and nonce-bound post-load scroll restoration | 07-01 Tasks 1-2; 07-02 Task 2; 07-03 Task 1 |
| CONTEXT | D-01 photo-led responsive layout | 07-01 Tasks 1-2, 07-03 Task 2 |
| CONTEXT | D-02 Make Model · Year | 07-01 Task 1, 07-03 Task 2 |
| CONTEXT | D-03 no similar listings | 07-02 Task 1, 07-03 Task 2 |
| CONTEXT | D-04 decorative heart | 07-02 Task 1, 07-03 Task 2 |
| CONTEXT | D-05 0/1 static and 2+ carousel/no thumbnails | 07-01 Task 2, 07-03 Task 2 |
| CONTEXT | D-06 exact supplied location | 07-01 Task 1, 07-03 Task 2 |
| CONTEXT | D-07 Read more description | 07-02 Task 1, 07-03 Task 2 |
| CONTEXT | D-08 offer-to-auth safe return | 07-02 Tasks 1-2, 07-03 Task 2 |
| CONTEXT | D-09 owner Edit listing | 07-02 Task 1, 07-03 Task 2 |
| CONTEXT | D-10 mobile sticky primary only | 07-02 Task 1, 07-03 Task 2 |
| CONTEXT | D-11 feed scroll continuity through browser/in-app Back | 07-03 Tasks 1-2 |

All in-scope GOAL, RESEARCH, and CONTEXT items are covered. Deferred ideas—functional saves, similar listings, messaging/contact, thumbnails, zoom, and richer trust/finance content—are intentionally absent.

## Artifacts this phase produces

- Updated `ListingCard`, `ListingDetail`, and `ListingsFeed` — semantic detail entry plus fail-closed nonce-bound return restoration.
- `src/lib/listing-return.test.ts` — safe auth-path plus nonce creation, exact claim, expiry, reuse, and consumption regression coverage.
- Phase SUMMARY evidence — automated release results and keyboard/mobile/back acceptance outcomes.

<output>
Create `.planning/phases/07-build-the-listing-detail-page/07-03-SUMMARY.md` when done.
</output>

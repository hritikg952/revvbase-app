---
phase: 07-build-the-listing-detail-page
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/components/listing-detail.tsx
  - src/lib/listing-detail-view.ts
  - src/lib/listing-detail-view.test.ts
  - src/lib/listing-return.ts
  - src/lib/listing-return.test.ts
  - src/components/auth-form.tsx
  - src/app/globals.css
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "A visitor can read a clamped description, reveal it intentionally, and begin the offer entry point without an offer being submitted."
    - "An anonymous visitor is returned only to the originating internal listing after successful sign-in or immediate sign-up; a listing owner sees Edit listing instead."
    - "Mobile keeps just the current primary action in the sticky action area and does not duplicate price or decorative controls."
  artifacts:
    - path: src/lib/listing-return.ts
      provides: Validated internal listing return-path resolver
    - path: src/lib/listing-detail-view.ts
      provides: Testable owner CTA and description display rules
    - path: src/components/auth-form.tsx
      provides: Auth completion redirect honoring validated returnTo
  key_links:
    - from: src/components/listing-detail.tsx
      to: src/lib/listing-return.ts
      via: Auth entry URL with a validated internal listing return target
    - from: src/components/auth-form.tsx
      to: src/lib/listing-return.ts
      via: Shared resolver after sign-in and immediate sign-up
---

<objective>
Expand the detail tracer with content disclosure and correct visitor-versus-owner actions, including a safe post-auth return to the listing.

Purpose: Turn the public detail into a useful offer entry while preserving the existing Auth/RLS boundaries and avoiding a messaging system.
Output: Description/CTA view rules, safe return-path helper, auth integration, focused tests, and mobile action styling.
</objective>

<execution_context>
@/Users/peakysmokin14/.codex/gsd-core/workflows/execute-plan.md
@/Users/peakysmokin14/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-build-the-listing-detail-page/07-CONTEXT.md
@.planning/phases/07-build-the-listing-detail-page/07-RESEARCH.md
@.planning/phases/07-build-the-listing-detail-page/07-01-SUMMARY.md
@src/components/auth-form.tsx
@src/components/auth-provider.tsx
@src/app/listings/[id]/edit/page.tsx
@src/components/listing-detail.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add description disclosure and owner-aware detail actions</name>
  <files>src/components/listing-detail.tsx, src/lib/listing-detail-view.ts, src/lib/listing-detail-view.test.ts, src/app/globals.css</files>
  <read_first>
    - src/components/listing-detail.tsx and 07-01-SUMMARY.md: extend the committed public tracer instead of creating a competing route/composition.
    - src/components/auth-provider.tsx and src/app/listings/[id]/edit/page.tsx: use the established asynchronous session and owner route patterns.
    - .planning/phases/07-build-the-listing-detail-page/07-CONTEXT.md: D-03, D-04, D-07, D-08, D-09, and D-10 are locked.
  </read_first>
  <behavior>
    - A long non-empty description starts clamped and the Read more control expands it; short/empty descriptions do not expose an unnecessary control (D-07).
    - Before AuthProvider resolves, action state is pending rather than briefly presenting the wrong owner/visitor action; an owner receives Edit listing and everyone else receives Make an offer (D-08, D-09).
    - The heart is visibly decorative and non-focusable; no similar-listings section, saved-listing action, message form, or contact action is rendered (D-03, D-04).
    - At the mobile breakpoint, the sticky bottom area contains only the resolved primary action and never another price display (D-10).
  </behavior>
  <action>Write failing tests for pure description-display and CTA-selection rules, then use them in `ListingDetail`. Implement D-07 with a semantic Read more/Read less control and CSS clamp state that keeps full text available after expansion. Implement D-08 and D-09 by waiting for `useAuth().loading`, comparing `user.id` with `listing.seller_id` only for presentation, linking owners to `/listings/{id}/edit`, and directing non-owners to the local auth entry built by the shared return helper. Keep the heart as an `aria-hidden` decorative icon or non-focusable visual element; it must not be an interactive control. Add the mobile sticky action treatment for D-10 with the resolved primary CTA only—no repeated price, heart, or secondary action—while preserving the desktop hero action area. Do not build similar listings, favourites, offer submission, buyer/seller messaging, phone/WhatsApp contact, finance, or trust content; RLS remains the authority for edit access.</action>
  <verify>
    <automated>npm test -- src/lib/listing-detail-view.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Owners never receive the offer CTA after auth resolution; visitors have a clear offer entry, descriptions disclose on demand, and the desktop/mobile layouts meet D-03, D-04, D-07, D-09, and D-10.</acceptance_criteria>
  <done>The detail page has the approved information hierarchy and an unambiguous primary action for each viewer state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve a validated listing return target through both auth outcomes</name>
  <files>src/lib/listing-return.ts, src/lib/listing-return.test.ts, src/components/auth-form.tsx</files>
  <read_first>
    - src/components/auth-form.tsx: retain its sign-in, immediate sign-up, confirmation-required, error, and pending semantics.
    - src/components/listing-detail.tsx and src/lib/listing-detail-view.ts from Task 1: use the same route target construction.
    - .planning/phases/07-build-the-listing-detail-page/07-RESEARCH.md: follow the internal-route return validation and open-redirect mitigation.
  </read_first>
  <behavior>
    - A valid internal detail pathname returns the user to that exact listing after password sign-in and after an immediate-session sign-up (D-08).
    - Empty, malformed, cross-origin, protocol-relative, query-manipulated, or non-listing return values resolve to `/`.
    - A sign-up awaiting email confirmation keeps its existing confirmation message and does not claim that a return happened.
  </behavior>
  <action>Create the shared pure resolver and test it before updating auth. Accept only a single internal detail pathname with one non-empty listing segment and no origin, query, fragment, duplicate slash, or encoded path escape; resolve every other value to `/`. Read `returnTo` with the App Router search-parameter API, use the resolver for both password sign-in success and the branch where sign-up supplies an immediate session, and navigate with the established router after the Auth response. Keep confirmation-required sign-up feedback in place until a future authenticated visit; do not add offer persistence, message state, external redirect support, or client-side authorization claims.</action>
  <verify>
    <automated>npm test -- src/lib/listing-return.test.ts &amp;&amp; npm run typecheck</automated>
    <human-check>While signed out, open a listing, activate Make an offer, sign in, and confirm the browser lands on the same listing. Repeat with Create account in an environment that supplies an immediate session; verify a crafted external return query instead lands on the marketplace root.</human-check>
  </verify>
  <acceptance_criteria>Auth completion honors only an internal listing return path in both immediate success paths, and anonymous offer entry does not create or imply an offer record.</acceptance_criteria>
  <done>Signed-out offer entry is safely round-tripped to the listing, while owner editing retains the existing protected route.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Detail CTA → Auth query input | A URL parameter controls the next browser location after authentication. |
| Browser owner indication → protected edit route | UI presentation must never become authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-04 | Tampering | returnTo query parameter | high | mitigate | Shared strict internal-detail resolver and regression cases for external/malformed values. |
| T-07-05 | Elevation of privilege | owner Edit listing affordance | high | mitigate | Wait for AuthProvider, treat comparison as presentation only, and retain existing owner/RLS query enforcement. |
| T-07-06 | Spoofing | decorative heart | low | mitigate | Render a non-focusable visual rather than an action that suggests saved-listing capability. |
</threat_model>

<verification>
Run both focused test files, typecheck, and the stated signed-out/owner browser checks. Confirm the sticky action at a viewport no wider than 600px.
</verification>

<success_criteria>
Visitors can move from a listing into the existing auth flow and safely return; owners see the correct edit affordance; all non-offer communication and saved-listing scope remains absent.
</success_criteria>

## Artifacts this phase produces

- `src/lib/listing-detail-view.ts` — deterministic content/action decisions.
- `src/lib/listing-return.ts` — safe return-target resolution shared by detail and Auth.
- Updated detail/auth components and responsive CSS — owner/visitor actions, description disclosure, and mobile sticky primary CTA.

<output>
Create `.planning/phases/07-build-the-listing-detail-page/07-02-SUMMARY.md` when done.
</output>

# Negotiation chat summary

Implemented the buyer/seller offer-first negotiation flow with Supabase RLS, atomic booking/reopening RPCs, a public Booked state, responsive inbox/chat route, listing offer sheet, and header message preview. Added SQL RLS coverage for negotiation permissions and lifecycle transitions.

Validation passed: `npm run typecheck`, `npm test`, and `npm run build`.

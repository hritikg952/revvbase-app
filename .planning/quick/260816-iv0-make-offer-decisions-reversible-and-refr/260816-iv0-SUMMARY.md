# Reversible offer decisions and unread badge refresh

Added protected Supabase RPCs for restoring a declined offer and undoing an accepted offer, surfaced corresponding thread controls, and synchronized the header unread badge after marking a conversation read.

Validation passed: `npm run typecheck`, `npm test`, and `npm run build`.

# Reversible offer decisions and unread badge refresh

Allow the actor who declined an offer to restore it, allow the accepting seller to undo a booking, and broadcast a local conversation-change event after read state is persisted so the header badge refreshes immediately.
